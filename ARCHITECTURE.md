# Panda Admin 技术架构文档

## 版本信息

- **版本**: v1.0.0 (正式版)
- **最后更新**: 2024
- **状态**: 已评审并确认
- **文档类型**: 技术架构文档

---

## 1. 项目概述

### 1.1 项目简介

Panda Admin 是一个基于 AI Agent 驱动的现代化管理后台框架，旨在通过人工智能技术简化前端开发流程，提高开发效率。项目基于 Ant Design Vue 组件库和 vue-vben-admin 架构设计，提供了一套完整的 AI 驱动开发解决方案。

### 1.2 核心目标

- **降低开发门槛**: 通过 AI Agent 辅助，使开发者能够快速构建复杂的管理界面
- **提高开发效率**: 自动化生成常用页面和组件代码，减少重复性工作
- **保持架构灵活性**: 支持多种 AI 驱动模式，适应不同场景需求
- **确保代码质量**: 生成的代码遵循最佳实践和设计规范

### 1.3 技术栈

| 类别 | 技术选型 | 版本要求 |
|------|---------|---------|
| 前端框架 | Vue.js | 3.x |
| UI 组件库 | Ant Design Vue | 4.x |
| 构建工具 | Vite | 5.x |
| 状态管理 | Pinia | 2.x |
| 路由管理 | Vue Router | 4.x |
| TypeScript | TypeScript | 5.x |
| HTTP 客户端 | Axios | 1.x |
| AI 集成 | OpenAI API / 自定义 AI Agent | - |

---

## 2. 整体架构设计

### 2.1 架构分层

```
┌─────────────────────────────────────────────────────────────┐
│                        表现层 (Presentation Layer)            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  页面组件     │  │  业务组件     │  │  布局组件     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      业务逻辑层 (Business Layer)             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  路由管理     │  │  状态管理     │  │  权限控制     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                        AI 驱动层 (AI Layer)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  AI Agent    │  │  代码生成器   │  │  Schema解析器 │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      数据访问层 (Data Access Layer)          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  API 封装     │  │  数据缓存     │  │  数据转换     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      基础设施层 (Infrastructure Layer)        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  工具函数     │  │  常量定义     │  │  类型定义     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 核心设计理念

#### 2.2.1 AI 驱动模式选择

经过充分的技术评估和讨论，Panda Admin 采用 **"配置驱动 + 代码生成"** 的混合模式：

**选择理由**：

1. **配置驱动**的优势：
   - 运行时灵活，无需重新部署即可调整界面
   - 适合动态场景，如权限控制、多租户定制
   - 降低对生成代码的维护成本

2. **代码生成**的优势：
   - 性能更优，直接编译为原生代码
   - 便于调试和二次开发
   - 可以集成复杂的业务逻辑

3. **混合模式**的价值：
   - 结合两者优势，在不同场景使用不同策略
   - 简单页面使用配置驱动，复杂页面使用代码生成
   - 提供灵活的切换机制

#### 2.2.2 AI Agent 交互方式

AI Agent 通过以下方式与前端框架交互：

```
用户输入 → AI Agent → JSON Schema → 解析器 → 配置/代码 → 页面渲染
```

**交互流程**：

1. **需求理解**: 用户通过自然语言描述页面需求
2. **意图识别**: AI Agent 分析需求，识别页面类型和功能
3. **Schema 生成**: 根据需求生成符合规范的 JSON Schema
4. **解析转换**: 前端解析器将 Schema 转换为配置对象或代码
5. **渲染展示**: 根据配置或代码渲染最终页面

---

## 3. 目录结构设计

### 3.1 标准目录结构

```
panda-admin/
├── public/                      # 静态资源
│   └── favicon.ico
├── src/
│   ├── ai/                      # AI 驱动核心模块
│   │   ├── agent/               # AI Agent 实现
│   │   │   ├── index.ts         # Agent 入口
│   │   │   ├── prompt.ts        # 提示词管理
│   │   │   └── types.ts         # Agent 类型定义
│   │   ├── generator/           # 代码生成器
│   │   │   ├── page.ts          # 页面生成器
│   │   │   ├── component.ts     # 组件生成器
│   │   │   └── api.ts           # API 生成器
│   │   ├── parser/              # Schema 解析器
│   │   │   ├── index.ts         # 解析器入口
│   │   │   ├── form.ts          # 表单解析
│   │   │   ├── table.ts         # 表格解析
│   │   │   └── layout.ts        # 布局解析
│   │   └── schema/              # JSON Schema 定义
│   │       ├── index.ts         # Schema 导出
│   │       ├── common.ts        # 通用 Schema
│   │       └── validators.ts    # 校验规则
│   ├── api/                     # API 接口
│   │   ├── index.ts             # API 入口
│   │   ├── request.ts           # 请求封装
│   │   └── modules/             # 业务模块 API
│   ├── assets/                  # 资源文件
│   │   ├── images/              # 图片
│   │   ├── styles/              # 样式文件
│   │   └── icons/               # 图标
│   ├── components/              # 公共组件
│   │   ├── AiAssistant/         # AI 助手组件
│   │   ├── PageGenerator/       # 页面生成器组件
│   │   └── ...
│   ├── composables/             # 组合式函数
│   │   ├── useAiAgent.ts        # AI Agent Hook
│   │   ├── useSchemaParser.ts   # Schema 解析 Hook
│   │   └── ...
│   ├── layouts/                 # 布局组件
│   │   ├── BasicLayout.vue      # 基础布局
│   │   └── BlankLayout.vue      # 空白布局
│   ├── router/                  # 路由配置
│   │   ├── index.ts             # 路由入口
│   │   ├── routes.ts            # 路由定义
│   │   └── guards.ts            # 路由守卫
│   ├── stores/                  # 状态管理 (Pinia)
│   │   ├── index.ts             # Store 入口
│   │   ├── app.ts               # 应用状态
│   │   ├── user.ts              # 用户状态
│   │   └── ai.ts                # AI 状态
│   ├── utils/                   # 工具函数
│   │   ├── index.ts             # 工具入口
│   │   ├── format.ts            # 格式化工具
│   │   └── validate.ts          # 校验工具
│   ├── views/                   # 页面视图
│   │   ├── dashboard/           # 仪表盘
│   │   ├── ai-studio/           # AI 工作台
│   │   └── ...
│   ├── App.vue                  # 根组件
│   └── main.ts                  # 应用入口
├── tests/                       # 测试文件
│   ├── unit/                    # 单元测试
│   └── e2e/                     # 端到端测试
├── types/                       # 类型定义
│   ├── global.d.ts              # 全局类型
│   ├── ai.d.ts                  # AI 相关类型
│   └── components.d.ts          # 组件类型
├── .env.development             # 开发环境变量
├── .env.production              # 生产环境变量
├── .eslintrc.js                 # ESLint 配置
├── .prettierrc.js               # Prettier 配置
├── tsconfig.json                # TypeScript 配置
├── vite.config.ts               # Vite 配置
└── package.json                 # 项目配置
```

### 3.2 目录结构说明

#### 3.2.1 AI 模块 (src/ai/)

这是 Panda Admin 的核心创新模块，负责所有 AI 相关功能：

- **agent/**: AI Agent 实现，负责与 AI 服务交互
- **generator/**: 代码生成器，将 Schema 转换为 Vue 代码
- **parser/**: Schema 解析器，将 JSON Schema 转换为配置对象
- **schema/**: JSON Schema 定义和校验
│#### 3.2.2 组件设计 (src/components/)

组件分为三类：

1. **基础组件**: 基于 Ant Design Vue 的二次封装
2. **业务组件**: 封装常用业务场景
3. **AI 组件**: AI 相关的交互组件

---

## 4. 数据交互协议

### 4.1 JSON Schema 规范

Panda Admin 使用 JSON Schema 作为 AI Agent 输出的标准格式。

#### 4.1.1 基础 Schema 结构

```typescript
interface PageSchema {
  version: string;           // Schema 版本
  type: 'page' | 'component'; // 类型
  meta: {
    title: string;           // 页面标题
    description?: string;    // 描述
    author?: string;         // 作者
    createdAt?: string;      // 创建时间
